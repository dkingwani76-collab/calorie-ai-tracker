package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Professional Polish Theme System (Light Warm Cream Aesthetic)
val DeepOnyxBg = Color(0xFFFDF8F6)       // Soft, organic warm cream background
val ContentSurface = Color(0xFFFFFFFF)   // Pure white card and container surface
val CardBorder = Color(0xFFF1F5F9)       // Very light slate/gray card borders

// Accents & Brand Elements
val EmeraldPrimary = Color(0xFF6750A4)     // Elegant Material 3 Purplish Indigo
val StreakActiveColor = Color(0xFF6750A4)  // Streak text highlight color
val LightStreakBg = Color(0xFFF3E7FF)      // Warm violet background container for Streak
val StreakBorderColor = Color(0xFFD0BCFF)  // Streak lavender border
val ActivePillBg = Color(0xFFEADDFF)       // Tab active pill background color

// Scanner Card Aesthetic (Maintains dark contrast on the center button)
val ScannerDarkBg = Color(0xFF211F26)      // Sleek charcoal card background
val ScannerCircleAccent = Color(0xFFD0BCFF) // Light lavender circle inside the scanner

// Macro Color Profiles (Vibrant slate color palette)
val MacroProtein = Color(0xFF3B82F6) // Blue 500
val MacroCarbs = Color(0xFF10B981)   // Emerald 500
val MacroFat = Color(0xFFF97316)     // Orange 500

// Text Colors (Premium deep slate high contrast scale)
val TextPrimary = Color(0xFF000000)   // Bold high-contrast Black
val TextSecondary = Color(0xFF1E293B) // Much darker charcoal (Slate 800) for readable body and info
val TextMuted = Color(0xFF475569)     // High-contrast deep slate (Slate 600) for labels and placeholders
